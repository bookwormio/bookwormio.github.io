import APIDisplay from "../components/API/APIDisplay";
import React from "react";

const API = () => {
  return (
    <div className="App">
      <header className="App-header">
        <APIDisplay />
      </header>
    </div>
  );
};

export default API;
